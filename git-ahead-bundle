#!/bin/sh

if [ $# -ne 0 ]
then
	echo "Usage: $0" >&2
	exit 1
fi

tlog=/tmp/bundle.$$
echo > $tlog
trap "rm -f $tlog" EXIT

git-ahead | while read b n
do
	f="$(basename "$(pwd)").$(echo "$b" | tr / _)".bundle

	git bundle create "$f" origin/"$b".."$b"
	echo "wrote $f" >> $tlog
done

cat $tlog
